{
  "scripts": {
    "serve": "yarn bundle && http-server dist",
    "start": "npx parcel ./public/index.html",
    "test": "spago test",
    "watch": "spago bundle-app --watch --to=./public/bundle.js"
  },
  "dependencies": {
    "strings": "^0.4.2",
    "uuid": "^3.3.3",
    "uuid-validate": "0.0.3"
  },
  "devDependencies": {
    "concurrently": "^4.1.2",
    "parcel": "^1.12.3"
  }
}
